---
/**
 * No hay tablero. Entrar es abrir el catálogo, que es donde está el trabajo.
 *
 * Lo que había aquí —cifras, huecos de numeración, listas de avisos— era una
 * pantalla que había que atravesar cada vez para llegar a lo que se venía a
 * hacer, y que además contaba problemas que nadie iba a arreglar desde ahí.
 */
export const prerender = false;
return Astro.redirect('/catalogo', 302);
---
